@model IEnumerable<DangDuyHien0230.Models.DDHHocsinhgioi>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DDHSinhvien)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DDHSinhvien.Sinhvien)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Ketqua">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Ketqua">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Ketqua">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
